<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gamedoikhang">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameDoiKhang">
        <activity android:name=".Test"></activity>
        <activity android:name=".GameOnlineActivity" />
        <activity android:name=".GameOfflineActivity" />
        <activity android:name=".LogAccountActivity" />
        <activity android:name=".CreateRoomActivity" />
        <activity android:name=".ListRoomActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>